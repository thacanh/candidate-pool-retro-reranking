* text=auto eol=lf

*.cmd -text
*.ps1 -text

# Frozen numerical artifacts and the transport ledgers are byte-preserving.
outputs/** -text
checksums.sha256 -text
release_manifest.json -text

*.eps binary
*.pdf binary
*.png binary
*.tif binary
*.tiff binary
